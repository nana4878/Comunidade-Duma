<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Duma — A comunidade que entrega o melhor marketing do Brasil</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,400..800&family=Instrument+Sans:ital,wght@0,400;0,500;0,600;1,400&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">
<style>
  :root{
    --ink:#161220;
    --ink-2:#1f1a2c;
    --paper:#f6f1e6;
    --paper-dim:#cfc7b8;
    --coral:#ef4a75;
    --gold:#f2b632;
    --teal:#1f8f83;
    --line: rgba(246,241,230,0.14);
    --radius: 18px;
    --maxw: 1180px;
    --ff-display: "Bricolage Grotesque", sans-serif;
    --ff-body: "Instrument Sans", sans-serif;
    --ff-mono: "Space Mono", monospace;
  }

  *{box-sizing:border-box;}
  html{scroll-behavior:smooth;}
  body{
    margin:0;
    background:var(--ink);
    color:var(--paper);
    font-family:var(--ff-body);
    line-height:1.55;
    -webkit-font-smoothing:antialiased;
  }
  img,svg{display:block;max-width:100%;}
  a{color:inherit;}
  h1,h2,h3{font-family:var(--ff-display);margin:0;letter-spacing:-0.01em;}
  p{margin:0;}
  .wrap{max-width:var(--maxw);margin:0 auto;padding:0 28px;}
  .eyebrow{
    font-family:var(--ff-mono);
    font-size:0.72rem;
    text-transform:uppercase;
    letter-spacing:0.16em;
    color:var(--paper-dim);
    display:inline-flex;
    align-items:center;
    gap:10px;
  }
  .eyebrow::before{
    content:"";
    width:7px;height:7px;border-radius:2px;
    background:var(--coral);
    display:inline-block;
    transform:rotate(45deg);
  }
  section{padding:96px 0;position:relative;}
  @media (max-width:720px){ section{padding:64px 0;} .wrap{padding:0 20px;} }

  :focus-visible{outline:2px solid var(--gold);outline-offset:3px;}

  /* ---------- BUTTONS ---------- */
  .btn{
    font-family:var(--ff-body);
    font-weight:600;
    font-size:0.98rem;
    display:inline-flex;
    align-items:center;
    gap:10px;
    padding:15px 26px;
    border-radius:999px;
    border:none;
    cursor:pointer;
    text-decoration:none;
    transition:transform .18s ease, box-shadow .18s ease;
  }
  .btn:hover{transform:translateY(-2px);}
  .btn-primary{
    background:var(--coral);
    color:#1a0e14;
    box-shadow:0 10px 30px -10px rgba(239,74,117,0.6);
  }
  .btn-primary:hover{box-shadow:0 14px 34px -8px rgba(239,74,117,0.75);}
  .btn-ghost{
    background:transparent;
    color:var(--paper);
    border:1px solid var(--line);
  }
  .btn-ghost:hover{border-color:var(--gold);}

  /* ---------- NAV ---------- */
  header.site{
    position:sticky;top:0;z-index:50;
    background:rgba(22,18,32,0.82);
    backdrop-filter:blur(10px);
    border-bottom:1px solid var(--line);
  }
  header.site .wrap{
    display:flex;align-items:center;justify-content:space-between;
    padding-top:18px;padding-bottom:18px;
  }
  .logo{
    font-family:var(--ff-display);
    font-weight:700;
    font-size:1.35rem;
    letter-spacing:-0.02em;
  }
  .logo span{color:var(--coral);}
  nav.links{display:flex;gap:28px;font-size:0.92rem;}
  nav.links a{text-decoration:none;color:var(--paper-dim);transition:color .15s;}
  nav.links a:hover{color:var(--paper);}
  @media (max-width:780px){ nav.links{display:none;} }

  /* ---------- HERO ---------- */
  .hero{padding-top:88px;padding-bottom:60px;overflow:hidden;}
  .hero .wrap{
    display:grid;
    grid-template-columns:1.05fr 0.95fr;
    gap:56px;
    align-items:center;
  }
  @media (max-width:940px){ .hero .wrap{grid-template-columns:1fr;} }

  .hero h1{
    font-size:clamp(2.3rem, 4.4vw, 3.6rem);
    font-weight:700;
    line-height:1.06;
    margin:20px 0 24px;
  }
  .hero h1 em{
    font-style:normal;
    color:var(--coral);
  }
  .hero p.lead{
    font-size:1.08rem;
    color:var(--paper-dim);
    max-width:52ch;
    margin-bottom:34px;
  }

  /* mosaic signature visual */
  .mosaic{
    position:relative;
    aspect-ratio:1/1;
    max-width:480px;
    margin:0 auto;
  }
  .tile{
    position:absolute;
    border-radius:16px;
    display:flex;
    align-items:flex-end;
    padding:14px;
    font-family:var(--ff-mono);
    font-size:0.72rem;
    text-transform:uppercase;
    letter-spacing:0.08em;
    color:rgba(22,18,32,0.75);
    font-weight:700;
    box-shadow:0 18px 30px -14px rgba(0,0,0,0.55);
    opacity:0;
    animation:settle .9s cubic-bezier(.2,.8,.2,1) forwards;
  }
  @keyframes settle{
    from{opacity:0; transform:translate(var(--fx,0),var(--fy,0)) rotate(var(--fr,0deg)) scale(.85);}
    to{opacity:1; transform:translate(0,0) rotate(var(--r,0deg)) scale(1);}
  }
  .t1{background:var(--coral); width:44%; height:40%; top:2%; left:4%;  --r:-6deg; --fx:-40px; --fy:-30px; --fr:-24deg;}
  .t2{background:var(--gold);  width:38%; height:32%; top:6%; right:2%; --r:5deg;  --fx:40px;  --fy:-20px; --fr:20deg; animation-delay:.08s;}
  .t3{background:var(--teal);  width:34%; height:34%; bottom:30%; left:0%; --r:4deg; --fx:-50px; --fy:20px; --fr:16deg; animation-delay:.16s; color:rgba(246,241,230,0.85);}
  .t4{background:var(--paper); width:40%; height:38%; bottom:26%; right:4%; --r:-4deg; --fx:44px; --fy:26px; --fr:-18deg; animation-delay:.24s;}
  .t5{background:var(--gold);  width:30%; height:26%; bottom:0%; left:16%; --r:6deg; --fx:-20px; --fy:44px; --fr:22deg; animation-delay:.32s;}
  .t6{background:var(--coral); width:32%; height:28%; bottom:2%; right:14%; --r:-8deg; --fx:24px; --fy:46px; --fr:-20deg; animation-delay:.4s;}
  @media (prefers-reduced-motion: reduce){
    .tile{animation:none; opacity:1; transform:rotate(var(--r,0deg));}
  }

  /* ---------- TEAM ---------- */
  .section-head{max-width:640px;margin-bottom:44px;}
  .section-head h2{font-size:clamp(1.7rem,3vw,2.4rem);margin-top:16px;}
  .section-head p{color:var(--paper-dim);margin-top:16px;font-size:1.02rem;}

  .team-scroll{
    display:flex;gap:20px;overflow-x:auto;padding:6px 2px 26px;
    scroll-snap-type:x proximity;
    -ms-overflow-style:none; scrollbar-width:thin;
  }
  .team-card{
    scroll-snap-align:start;
    flex:0 0 240px;
    background:var(--ink-2);
    border:1px solid var(--line);
    border-radius:var(--radius);
    padding:22px;
    transition:transform .2s ease, border-color .2s ease;
  }
  .team-card:hover{transform:translateY(-6px);border-color:var(--gold);}
  .avatar{
    width:64px;height:64px;border-radius:14px;
    display:flex;align-items:center;justify-content:center;
    font-family:var(--ff-display);font-weight:700;font-size:1.3rem;
    color:rgba(22,18,32,0.82);
    margin-bottom:18px;
  }
  .team-card h3{font-size:1.12rem;}
  .team-card .role{
    font-family:var(--ff-mono);font-size:0.7rem;text-transform:uppercase;
    letter-spacing:0.08em;color:var(--coral);margin:8px 0 12px;
  }
  .team-card p{color:var(--paper-dim);font-size:0.92rem;}

  .team-outro{
    display:flex;align-items:center;justify-content:space-between;
    gap:24px;flex-wrap:wrap;
    margin-top:32px;padding-top:32px;border-top:1px solid var(--line);
  }
  .team-outro p{max-width:46ch;color:var(--paper-dim);}

  /* ---------- SERVICES ---------- */
  .services-grid{
    display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-bottom:40px;
  }
  @media (max-width:860px){ .services-grid{grid-template-columns:repeat(2,1fr);} }
  @media (max-width:560px){ .services-grid{grid-template-columns:1fr;} }
  .service{
    display:flex;align-items:center;gap:14px;
    background:var(--ink-2);border:1px solid var(--line);
    border-radius:14px;padding:18px 20px;font-size:0.98rem;
  }
  .service .chip{
    width:14px;height:14px;border-radius:4px;flex:none;transform:rotate(45deg);
  }
  .services-foot{
    display:flex;align-items:center;justify-content:space-between;gap:24px;flex-wrap:wrap;
  }
  .services-foot p{max-width:56ch;color:var(--paper-dim);}

  /* ---------- HISTORY ---------- */
  .history .wrap{
    display:grid;grid-template-columns:1.1fr 0.9fr;gap:56px;align-items:center;
  }
  @media (max-width:900px){ .history .wrap{grid-template-columns:1fr;} }
  .history p{color:var(--paper-dim);font-size:1.03rem;margin-top:18px;}
  .history .pillars{margin-top:26px;display:flex;flex-direction:column;gap:14px;}
  .pillar{display:flex;gap:14px;align-items:flex-start;}
  .pillar .mark{
    width:22px;height:22px;border-radius:6px;background:var(--teal);
    flex:none;margin-top:3px;
  }
  .institutional{
    aspect-ratio:4/5;border-radius:22px;
    background:linear-gradient(155deg, var(--coral) 0%, var(--ink-2) 45%, var(--teal) 100%);
    position:relative;overflow:hidden;
    border:1px solid var(--line);
    display:flex;align-items:flex-end;padding:24px;
  }
  .institutional span{
    font-family:var(--ff-mono);font-size:0.72rem;text-transform:uppercase;
    letter-spacing:0.1em;color:rgba(246,241,230,0.75);
  }

  /* ---------- FOUNDER QUOTE ---------- */
  .founder{background:var(--paper);color:var(--ink);}
  .founder .wrap{
    display:grid;grid-template-columns:0.32fr 0.68fr;gap:48px;align-items:center;
  }
  @media (max-width:820px){ .founder .wrap{grid-template-columns:1fr;} }
  .founder-avatar{
    width:150px;height:150px;border-radius:22px;
    background:var(--ink);color:var(--paper);
    display:flex;align-items:center;justify-content:center;
    font-family:var(--ff-display);font-weight:700;font-size:2.4rem;
    transform:rotate(-3deg);
  }
  .quote-mark{
    font-family:var(--ff-display);font-size:3.4rem;color:var(--coral);
    line-height:1;margin-bottom:6px;
  }
  blockquote{
    margin:0;font-family:var(--ff-display);font-weight:500;
    font-size:clamp(1.25rem,2.2vw,1.7rem);line-height:1.35;
  }
  .founder cite{
    display:block;margin-top:20px;font-style:normal;
    font-family:var(--ff-mono);font-size:0.8rem;text-transform:uppercase;
    letter-spacing:0.06em;color:#6a6355;
  }

  /* ---------- PORTFOLIO ---------- */
  .portfolio .wrap{
    background:var(--ink-2);border:1px solid var(--line);border-radius:26px;
    padding:56px;display:flex;justify-content:space-between;align-items:center;gap:32px;flex-wrap:wrap;
  }
  .portfolio h2{font-size:clamp(1.5rem,2.6vw,2.1rem);max-width:20ch;}
  .portfolio p{color:var(--paper-dim);margin-top:14px;max-width:52ch;}
  .portfolio-copy{flex:1 1 380px;}

  /* ---------- SOCIAL ---------- */
  .social{text-align:center;}
  .social h2{font-size:clamp(1.5rem,2.8vw,2.15rem);max-width:24ch;margin:16px auto 34px;}
  .social-row{display:flex;justify-content:center;gap:16px;flex-wrap:wrap;}
  .social-btn{
    display:inline-flex;align-items:center;gap:10px;
    padding:14px 24px;border-radius:999px;
    border:1px solid var(--line);text-decoration:none;
    font-weight:600;font-size:0.95rem;transition:all .18s ease;
  }
  .social-btn:hover{border-color:var(--gold);transform:translateY(-2px);}
  .social-btn svg{width:18px;height:18px;}

  /* ---------- FOOTER ---------- */
  footer{
    border-top:1px solid var(--line);padding:34px 0;
    font-family:var(--ff-mono);font-size:0.78rem;color:var(--paper-dim);
    text-align:center;
  }
</style>
</head>
<body>

<header class="site">
  <div class="wrap">
    <div class="logo">du<span>ma</span></div>
    <nav class="links">
      <a href="#equipe">Equipe</a>
      <a href="#servicos">Serviços</a>
      <a href="#historia">História</a>
      <a href="#portfolio">Portfólio</a>
    </nav>
    <a class="btn btn-ghost" href="https://wa.me/5521999999999" target="_blank" rel="noopener">Fale com a gente</a>
  </div>
</header>

<!-- ================= HERO ================= -->
<section class="hero">
  <div class="wrap">
    <div>
      <span class="eyebrow">Comunidade colaborativa de marketing</span>
      <h1>Somos a <em>Duma</em> — a comunidade daqueles que se juntaram para entregar o melhor marketing do Brasil.</h1>
      <p class="lead">Somos um grupo de profissionais independentes da área do marketing que se juntaram para criar a Duma, uma comunidade colaborativa que se propõe a entregar qualquer ideia, serviço ou produto com qualidade máxima.</p>
      <a class="btn btn-primary" href="https://wa.me/5521999999999" target="_blank" rel="noopener">
        Fale com um especialista no WhatsApp
      </a>
    </div>
    <div class="mosaic" aria-hidden="true">
      <div class="tile t1">Design</div>
      <div class="tile t2">Vídeo</div>
      <div class="tile t3">Social</div>
      <div class="tile t4">Tráfego</div>
      <div class="tile t5">Copy</div>
      <div class="tile t6">Branding</div>
    </div>
  </div>
</section>

<!-- ================= EQUIPE ================= -->
<section id="equipe">
  <div class="wrap">
    <div class="section-head">
      <span class="eyebrow">Quem entrega</span>
      <h2>Nossa equipe</h2>
      <p>Nossa equipe é formada por Publicitários, Designers gráficos, Videomakers, Ilustradores, Animadores, Social Media, Marketeiros, Copy, Influenciadores, Fotógrafos e muitos outros. Conheça as lideranças do nosso time:</p>
    </div>

    <div class="team-scroll" role="list">
      <!-- cards generated below -->
      <article class="team-card" role="listitem">
        <div class="avatar" style="background:var(--coral)">R</div>
        <h3>Robson</h3>
        <div class="role">Diretor Criativo</div>
        <p>Cuida do conteúdo gráfico dentro e fora das redes.</p>
      </article>
      <article class="team-card" role="listitem">
        <div class="avatar" style="background:var(--gold)">I</div>
        <h3>Igor</h3>
        <div class="role">Diretor Audiovisual</div>
        <p>Organiza a produção de vídeos em todos os formatos.</p>
      </article>
      <article class="team-card" role="listitem">
        <div class="avatar" style="background:var(--teal)">E</div>
        <h3>Estela</h3>
        <div class="role">Diretora de Social Media</div>
        <p>Planejamento estratégico de conteúdo.</p>
      </article>
      <article class="team-card" role="listitem">
        <div class="avatar" style="background:var(--coral)">B</div>
        <h3>Bruno</h3>
        <div class="role">Diretor de Tecnologia</div>
        <p>Organiza o desenvolvimento de Software.</p>
      </article>
      <article class="team-card" role="listitem">
        <div class="avatar" style="background:var(--gold)">G</div>
        <h3>Guilherme</h3>
        <div class="role">Diretor de Projetos</div>
        <p>Lançamentos, campanhas e produtos.</p>
      </article>
      <article class="team-card" role="listitem">
        <div class="avatar" style="background:var(--teal)">G</div>
        <h3>Gabriel</h3>
        <div class="role">Diretor Editorial</div>
        <p>Roteiros, conteúdo e comunicação.</p>
      </article>
      <article class="team-card" role="listitem">
        <div class="avatar" style="background:var(--coral)">J</div>
        <h3>Jeterson</h3>
        <div class="role">Diretor Operacional</div>
        <p>Gestão de time, equipe e posicionamento.</p>
      </article>
    </div>

    <div class="team-outro">
      <p>Nossa equipe é composta por vários profissionais com especializações diferentes, prontos pra atender sua demanda agora mesmo!</p>
      <a class="btn btn-primary" href="https://wa.me/5521999999999" target="_blank" rel="noopener">Conta pra gente do que precisa</a>
    </div>
  </div>
</section>

<!-- ================= SERVIÇOS ================= -->
<section id="servicos">
  <div class="wrap">
    <div class="section-head">
      <span class="eyebrow">O que fazemos</span>
      <h2>Alguns serviços que você pode encontrar com a gente</h2>
    </div>

    <div class="services-grid">
      <div class="service"><span class="chip" style="background:var(--coral)"></span>Gestão de rede social</div>
      <div class="service"><span class="chip" style="background:var(--gold)"></span>Criativos para campanhas</div>
      <div class="service"><span class="chip" style="background:var(--teal)"></span>Edição de vídeo</div>
      <div class="service"><span class="chip" style="background:var(--coral)"></span>Design de produtos</div>
      <div class="service"><span class="chip" style="background:var(--gold)"></span>Criação de site ou aplicativo</div>
      <div class="service"><span class="chip" style="background:var(--teal)"></span>Tráfego pago</div>
      <div class="service"><span class="chip" style="background:var(--coral)"></span>Criar campanhas do absoluto zero</div>
      <div class="service"><span class="chip" style="background:var(--gold)"></span>Branding e posicionamento de marca</div>
      <div class="service"><span class="chip" style="background:var(--teal)"></span>Identidade visual</div>
    </div>

    <div class="services-foot">
      <p>Nossos serviços podem ser feitos por um ou vários profissionais preparados para garantir performance e resultado. Fala pra gente qual seu projeto e vamos desenhar uma forma de torná-lo ainda mais lucrativo.</p>
      <a class="btn btn-primary" href="https://wa.me/5521999999999" target="_blank" rel="noopener">Quero dar esse passo</a>
    </div>
  </div>
</section>

<!-- ================= HISTÓRIA ================= -->
<section id="historia" class="history">
  <div class="wrap">
    <div>
      <span class="eyebrow">De onde viemos</span>
      <h2 style="font-size:clamp(1.7rem,3vw,2.4rem);margin-top:16px;">Nossa história</h2>
      <p>Fomos fundados para juntar todo o conhecimento do que há de melhor no marketing para fazer duas coisas principais:</p>
      <div class="pillars">
        <div class="pillar"><span class="mark" style="background:var(--coral)"></span>
          <p>Valorizar o pensamento humano capaz de pensar e criar projetos incríveis.</p>
        </div>
        <div class="pillar"><span class="mark" style="background:var(--gold)"></span>
          <p>Entregar essas ideias com a maior qualidade possível, juntando o que há de melhor no mercado.</p>
        </div>
      </div>
      <p>Hoje somos uma plataforma sólida tanto na entrega de conteúdo relevante quanto na execução eficiente, ajudando empresas, negócios, projetos e pessoas a atingirem seus objetivos.</p>
    </div>
    <div class="institutional">
      <span>Foto institucional — Equipe Duma</span>
    </div>
  </div>
</section>

<!-- ================= FUNDADOR ================= -->
<section class="founder">
  <div class="wrap">
    <div class="founder-avatar">JS</div>
    <div>
      <div class="quote-mark">“</div>
      <blockquote>Sou um entusiasta do pensamento humano, acredito que ideias valiosas merecem serem vistas e que todo ser humano é capaz de impactar o mundo quando se propõe a pensar e tem gente junto que acredita. É isso que construímos aqui na Duma.</blockquote>
      <cite>Jeterson Santos — Fundador e Diretor Operacional da Duma</cite>
    </div>
  </div>
</section>

<!-- ================= PORTFÓLIO ================= -->
<section id="portfolio" class="portfolio">
  <div class="wrap">
    <div class="portfolio-copy">
      <span class="eyebrow">Prova no trabalho</span>
      <h2>Conheça nosso trabalho mais de perto</h2>
      <p>Se você quiser avaliar as dezenas de ações que fizemos e levaram ideias rumo aos seus objetivos, clique no botão abaixo e te enviaremos um panorama completo da nossa qualidade de execução e nosso serviço.</p>
    </div>
    <a class="btn btn-primary" href="https://wa.me/5521999999999" target="_blank" rel="noopener">Quero ver essas possibilidades</a>
  </div>
</section>

<!-- ================= REDES SOCIAIS ================= -->
<section class="social">
  <div class="wrap">
    <span class="eyebrow">Fica de olho</span>
    <h2>Nos acompanhe nas redes sociais, trazemos conteúdo atualizado todo dia pra te ajudar nessa jornada.</h2>
    <div class="social-row">
      <a class="social-btn" href="https://instagram.com/" target="_blank" rel="noopener">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="5"/><circle cx="12" cy="12" r="4"/><circle cx="17.5" cy="6.5" r="1"/></svg>
        Instagram
      </a>
      <a class="social-btn" href="https://tiktok.com/" target="_blank" rel="noopener">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 3v11.5a3.5 3.5 0 1 1-3.5-3.5"/><path d="M14 3c0 3 2 5 5 5"/></svg>
        TikTok
      </a>
      <a class="social-btn" href="https://wa.me/5521999999999" target="_blank" rel="noopener">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
        WhatsApp
      </a>
    </div>
  </div>
</section>

<footer>
  Duma — A plataforma que conecta pessoas e ideias e se importa com isso!
</footer>

</body>
</html>
